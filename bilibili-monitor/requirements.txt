# B站监控核心依赖
yt-dlp>=2024.1.0
faster-whisper>=1.0.0
requests>=2.31.0
pyyaml>=6.0.1
python-dotenv>=1.0.0

# 数据存储
duckdb>=0.10.0
chromadb>=0.4.22

# faster-whisper 使用 CTranslate2，不需要 PyTorch
# GPU 模式（本地 RTX 4060）需要单独安装: pip install torch
