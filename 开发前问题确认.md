# 开发前问题确认

> 请在每个问题下方的 `> 回答：` 后面填写答案。

---

## 一、bilibili-monitor 迁移

### Q1：Hermes 平台改造方式

bilibili-monitor 目前是 Hermes 平台的 Skill，迁移到 Docker 独立运行时：
- 是需要从 Hermes 中抽出来改造成独立 Python 服务？
- 还是 Hermes 平台本身提供了 Docker 化的方式？
- 核心代码（WBI签名、yt-dlp下载、Whisper转写、Checkpoint）能否直接复用？

> 回答：
从 Hermes 中抽出来改造成独立，核心代码需要你阅读后判断

### Q2：已有 txt 文件的元数据

历史转写的 txt 文件里，除了纯文本内容之外，是否包含元数据（bvid、UP主名、标题、发布日期等）？如果只有纯文本，迁移入库时元数据从哪里获取？

> 回答：
只有txt，你可以开发一个参数，只获取元数据不转写，等全部获取完毕了在关闭这个选项


---

## 二、llm-summarizer

### Q3：当前形态

llm-summarizer 目前是：
- 一个独立的 HTTP API 服务（被 bilibili-monitor 通过 HTTP 调用）？
- 还是一个 Python 模块（被 bilibili-monitor 直接 import 调用）？

> 回答：
bilibili-monitor 后边看源码



### Q4：部署方式

如果它只在 bilibili-monitor 调用时才有工作，你打算：
- 设计成常驻 HTTP 服务（一直占内存，但解耦清晰）？
- 还是按需启动（`docker compose run --rm`，跑完释放内存）？

> 回答：
暂时设计成由ai例如hermes进行定时调用

---

## 三、代码仓库结构

### Q5：仓库策略

三个项目目前在 GitHub 上是三个独立仓库（bilibili-monitor、personal-knowledge-rag、text-to-sql）。后续开发你打算：
- 保持多仓，各自独立？
- 合并成 monorepo（一个仓库，Docker Compose 统一编排）？
- 还是保持多仓 + 一个额外的编排仓库（只放 docker-compose.yml 和 nginx 配置）？

未来准备合并，目前是各自独立

> 回答：

---

## 四、RAG 系统

### Q6：ChromaDB 实例

RAG 系统已经在 NAS 上用 Docker 跑着了，ChromaDB 里已有 100+ 篇文档。新的 `video_knowledge` collection 是：
- 加到同一个 ChromaDB 实例里（一个容器，两个 collection）？
- 还是另起一个新的 ChromaDB 容器？

> 回答：
旧的数据没有价值，我们重新生成一个容器，旧的制作参考



---

## 五、Router Agent

### Q7：Agent 框架选择

Text-to-SQL 用了 CrewAI 做 4-Agent 协作。Router Agent 的意图识别 + 查询分发相对简单，你打算：
- 也用 CrewAI？
- 还是用更轻量的方式（直接调 Minimax API 做分类 + Python 逻辑分发）？

> 回答：
根据代码的开发情况来决定



---

## 六、统一前端

### Q8：前端策略

Text-to-SQL 项目已经有一个 React 18 前端了。新的统一前端是：
- 在现有前端基础上扩展（加路由 Agent 对接 + 数据概览面板）？
- 还是另起一个全新的前端项目？

> 回答：
都可以，可以参考之前的内容

---

## 七、开发环境

### Q9：本地 Docker 环境

文档里写 Docker Desktop 是"待安装"。现在本地环境就绪了吗？
- Docker Desktop 装好了吗？WSL2 后端正常吗？
- NAS 上 Docker + Docker Compose 装好了吗？

> 回答：
NAS上有
windows还没安装

---

## 八、其他补充

### Q10：你还有其他需要我知道的信息吗？

比如：已有数据的存放路径、API 密钥的管理方式、NAS 的型号和 Docker 安装方式、Hermes 平台的技术细节等。

> 回答：
一边开发一边确认
