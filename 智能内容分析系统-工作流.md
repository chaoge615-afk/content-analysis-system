# 智能内容分析系统 - 工作流

> 本文档供 AI 助手（ChatGPT、Gemini、Claude 等）阅读，用于理解项目全貌并协助后续开发。
> 最后更新：2026-05-27

---

## 一、项目概述

一套从 B站视频采集到智能问答的端到端 AI 系统。

**核心链路**：B站情感博主视频 → 自动下载转写 → LLM 精炼 → 结构化入库(DuckDB) + 向量化入库(ChromaDB) → 统一入口智能问答

**作者**：李畅超
**GitHub**：https://github.com/chaoge615-afk/content-analysis-system

---

## 二、系统当前状态

### 整体进度：Phase 1-8、10 全部完成，Phase 9 进行中（153/154 任务已完成）

### 子项目状态

| 子项目 | 状态 | 说明 |
|--------|------|------|
| bilibili-monitor | ✅ 独立化改造完成 | 从 Hermes Skill 改造为独立 Python 服务，去 Hermes 依赖 |
| personal-knowledge-rag | ✅ 改造完成 | 视频知识库 RAG，BM25+向量混合检索，支持 DeepSeek/MiniMax 双 LLM |
| text-to-sql | ✅ 适配完成 | DuckDB schema 扩展，支持视频元数据查询 |
| router-agent | ✅ 开发完成 | 意图分类+查询分发+结果融合+UP主管理+ASR管理 |
| frontend | ✅ 开发完成 | React 对话+管理面板，采集触发/UP主管理/GPU转录/ASR转写/查询日志/系统监控 |
| Docker Compose | ✅ 编排完成 | 统一编排，profiles 区分 dev/nas，GPU 直通 |
| 数据迁移 | ✅ 完成 | 1413 个精炼文件已迁移（DuckDB: 1413条, ChromaDB: 1760块） |
| 端到端验证 | ✅ 通过 | 语义/结构化/混合查询/UP主添加/ASR转写 全部验证通过 |

### 关键改造记录

- **bilibili-monitor 独立化**：去除 Hermes 平台依赖，改为标准 Python 项目
- **Whisper 替换**：原版 Whisper → faster-whisper（CTranslate2，CPU 友好）
- **精炼集成**：refine_batch.py 抽取为 refiner.py 模块，集成到 bilibili-monitor
- **RAG 增强**：新增 video_knowledge collection，BM25+向量混合检索，metadata 过滤
- **个人笔记功能移除**：用户不需要 knowledge collection，只保留 video_knowledge
- **LLM 双提供者**：RAG 支持 DeepSeek（默认）和 MiniMax 两种 LLM
- **分类器修复**：意图分类器 prompt 修复，防止 LLM 幻觉不存在的 category
- **UP主管理**：前端输入 B站链接 → 解析预览 → 选模型 → 写入 YAML 配置
- **云 ASR 转写**：硅基流动 SenseVoiceSmall（免费）替代本地 Whisper，预算控制 + 手动开关
- **GPU 转录**：gpu-service 容器 + RTX 4060 直通，CUDA 加速转写
- **管理面板**：采集触发/Cookie管理/UP主管理/GPU转录/ASR/查询日志/系统监控

---

## 三、数据采集工作流

### 3.1 bilibili-monitor 全流程

```
[Cron 触发 · 每6小时 · bilibili-cron 容器]
[或 前端手动触发 · router-agent Docker SDK]
    ↓
monitor_all.py（入口脚本）
    ├── 扫描 config/*.yaml（UP主配置）
    ├── 支持 --asr 参数（强制启用云 ASR）
    ├── 分批执行（MAX_CONCURRENT_TRANSCRIBE=2）
    └── 每个 UP 主调用 monitor.py
         ↓
    monitor.py（单个UP主处理）
         ├── 1. get_up_videos.py → WBI API 拉取视频列表
         ├── 2. download_videos.py → yt-dlp 下载 m4a
         ├── 3. ┌──────────────────────────────────────────┐
         │      │ 转写分支（按 ASR 设置自动选择）            │
         │      │  ① transcribe_local.py                    │
         │      │     faster-whisper CPU/GPU → 语音转文字   │
         │      │  ② transcribe_asr.py                      │
         │      │     SiliconFlow SenseVoiceSmall（免费）    │
         │      │     预算检查 → multipart upload → 用量记录 │
         │      └──────────────────────────────────────────┘
         ├── 4. refiner.py → DeepSeek API 精炼 → 三段式摘要
         ├── 5. db_writer.py → 写入 DuckDB（video_meta 表）
         ├── 6. chroma_writer.py → 写入 ChromaDB（video_knowledge）
         └── 7. qq_notify.py → QQ Bot 推送通知

ASR 设置/用量（共享 bilibili-data 卷）：
  .asr_config/settings.json → { enabled, monthly_budget_minutes, model }
  .asr_config/usage.json    → { month, total_minutes, records: [...] }
```

### 3.2 精炼格式（三段式）

```
核心观点：
  - 视频的主要论点和建议

案例摘要：
  - 视频中提到的具体案例或故事

可行动建议：
  - 用户可以立即采取的行动步骤
```

### 3.3 分类体系（31个情感分类）

```
01_喜欢, 02_聊天, 03_撩妹, 04_筛选, 05_拒绝, 06_备胎, 07_修养, 08_婚姻,
09_推进, 10_分手, 11_话术, 12_深夜, 13_表白, 14_约会, 15_复合, 16_社交,
17_暧昧, 18_生理, 19_挽回, 20_出轨, 21_相亲, 22_追求, 23_回复, 24_心态,
25_情商, 26_吸引力, 27_恋爱技巧, 28_技巧, 29_感情, 30_两性知识, 31_自我提升, 32_两性健康
```

### 3.4 历史数据迁移（已完成）

```
migrate_refined.py 执行结果：
├── 扫描：1413 个精炼 txt 文件
├── 有 BVID：239 个（16%），调 B站 API 补全元数据
├── 无 BVID：1174 个（83%），标记 unknown
├── API 成功：235 个，失败：4 个
├── DuckDB：1413 条写入成功
├── ChromaDB：1760 个文档块
└── 耗时：约 10 分钟
```

### 3.5 UP主管理工作流

```
前端管理面板 → 采集触发 → UP主管理区域
    ↓
  输入 B站链接（两种格式）：
  ├── https://space.bilibili.com/<uid>     → 提取 UID
  └── https://www.bilibili.com/video/<bvid> → 提取 BVID → 调API获取 owner_mid
    ↓
  up_manager.parse_url() → 正则解析
  up_manager.fetch_profile() → B站 API
  ├── BVID → GET /x/web-interface/view?bvid=xxx → { owner.mid, owner.name, owner.face }
  └── UID  → GET /x/space/wbi/acc/info?mid=xxx → { name, face, mid }
    ↓
  前端展示预览卡片（名称、UID、头像）
    ↓
  选择 whisper_model（small / medium）
    ↓
  up_manager.add_up() → 生成 config/<uid>.yaml
    ↓
  下次 bilibili-monitor 扫描 config/*.yaml 时自动包含新 UP主
```

### 3.6 云 ASR 转写工作流

```
触发方式：
├── Cron 自动：bilibili-monitor 读取 .asr_config/settings.json
│   enabled=true 时自动走 ASR 分支
└── 前端手动：管理面板 → GPU转录 → 云ASR → 手动触发
    → POST /api/asr/transcribe → Docker SDK 启动 bilibili-monitor --asr

ASR 转写流程（transcribe_asr.py）：
├── 扫描 m4a 文件（增量：跳过已转写的）
├── 预算检查：已用分钟 ≥ 预算 → 跳过并记录日志
├── 文件大小检查：> 50MB → 跳过
├── POST https://api.siliconflow.cn/v1/audio/transcriptions
│   ├── multipart/form-data: file + model=FunAudioLLM/SenseVoiceSmall
│   ├── Authorization: Bearer $SILICONFLOW_API_KEY
│   └── 响应：{ "text": "转写文本" }
├── 写入 .txt 文件（与本地 Whisper 输出格式一致）
├── 更新 usage.json（累加时长 + 记录）
└── 后续流程不变：refiner → db_writer → chroma_writer → notify

成本控制：
├── 模型：FunAudioLLM/SenseVoiceSmall（免费）
├── 预算：月度分钟数上限（前端可设置）
├── 开关：前端 toggle 控制启用/禁用
└── 超额自动停止转写，不删除已转写内容
```

---

## 四、数据查询工作流

### 4.1 意图分类

```
用户问题
    ↓
  路由Agent（router-agent :8000）
    ├── 调用 MiniMax M2.7 做意图分析
    ├── 判断类型：structured / semantic / hybrid
    ├── 提取过滤条件：
    │   ├── up_name（UP主名称）
    │   ├── category（只能用 31 个有效分类名）
    │   ├── date_range（时间范围）
    │   └── keywords（话题关键词）
    └── 输出：路由决策 + 过滤条件
```

**重要：** category 是目录分类名（如 `01_喜欢`），不是话题关键词。"冷暴力"是话题（放 keywords），不是分类。

### 4.2 三种查询模式

```
① structured（结构化查询）→ Text-to-SQL
   - 涉及数量统计、时间排序、比较等
   - 示例："桃姐最近发了几个视频？"
   - 流程：问题 → SQL 生成 → DuckDB 查询 → 返回结果

② semantic（语义查询）→ RAG
   - 涉及观点、建议、内容理解等
   - 示例："博主们对冷暴力怎么看？"
   - 流程：问题 → BM25+向量混合检索 → metadata 过滤 → LLM 生成回答

③ hybrid（混合查询）→ SQL + RAG 并行
   - 同时包含结构化和语义需求
   - 示例："桃姐最近聊了什么？她关于吵架的建议？"
   - 流程：SQL 先筛 + RAG 检索 → LLM 融合回答
```

### 4.3 RAG 检索流程

```
用户问题
    ↓
  BM25 关键词检索（rank_cap=3）
    +
  向量相似度检索（top_k=5, SiliconFlow Embedding）
    ↓
  混合排序融合（HybridSearch）
    ↓
  metadata 过滤（up_name / category / bvid）
    ↓
  拼接上下文（附带来源信息：UP主、分类、BV号）
    ↓
  LLM 生成回答
  ├── DeepSeek V4 Pro（默认）
  └── MiniMax M2.7（备选）
```

---

## 五、部署环境

### 5.1 硬件环境对比

| 配置 | 本地电脑（开发） | NAS（生产） |
|------|-----------------|-------------|
| CPU | AMD Ryzen 7 8845H（8核16线程） | Intel N150（4核低功耗） |
| 内存 | 24GB | DDR4 8G（模块化，可扩展） |
| GPU | NVIDIA RTX 4060 Laptop（8GB 显存） | 无独显（集成核显） |
| Whisper | GPU 加速，秒级转写 | faster-whisper CPU，3-5分钟/条 |
| 用途 | 开发调试，下班后使用 | 7×24 生产运行，上班时远程开发 |
| Docker | Docker Desktop（WSL2 后端） | Docker Compose 统一编排 |

### 5.2 开发策略

```
           GitHub（中心仓库）
          ↗                    ↘
    本地电脑                NAS
  （下班后开发）          （上班时远程开发 / 生产运行）
  RTX 4060 · 24G         N150 · 8G
  GPU Whisper            CPU Whisper
```

核心原则：
- 所有代码必须经过 GitHub，禁止两边各自改不提交
- 环境差异走配置文件，不走代码分支
- 每天开工前 pull，收工前 push

### 5.3 Docker Compose profiles

```bash
# 开发环境（含 GPU 转录服务）
docker compose --profile dev up -d

# 生产环境（含定时调度）
docker compose --profile nas up -d

# 按需运行采集
docker compose run --rm bilibili-monitor

# 手动触发 ASR 转写
docker compose run --rm bilibili-monitor python scripts/monitor_all.py --asr
```

---

## 六、版本管理与开发流程

### 6.1 Git 分支策略

```
master        ← 稳定版本（NAS 生产环境跑这个）
dev           ← 日常开发（本地和NAS都在这个分支上改）
feature/xxx   ← 大功能单独拉分支（可选）
```

### 6.2 环境差异处理

代码层面完全一致，差异只体现在配置上：

#### `.env` 文件（不提交到 Git）

```env
# 本地 .env（开发环境）
WHISPER_MODEL=large-v3
WHISPER_DEVICE=cuda
COMPUTE_TYPE=float16

# NAS .env（生产环境）
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
COMPUTE_TYPE=int8
```

#### `.gitignore` 必须排除

```gitignore
.env
dev-data/
*.pyc
__pycache__/
venv/
node_modules/
```

### 6.3 每日工作流

```
上班前（本地）：
  git add → git commit → git push
  关掉本地，去上班

上班时（远程到NAS）：
  git pull
  改代码 → git commit → git push
  NAS 验证：docker compose --profile nas up -d

下班后（本地）：
  git pull（拉 NAS 上的改动）
  继续开发 → git commit → git push
  本地验证：docker compose --profile dev up
```

### 6.4 防冲突习惯

```
① 开工第一件事：git pull --rebase
② 改完一个功能就 commit，别攒着
③ 别两边同时改同一个文件
④ 如果本地用 Claude Code 改，NAS 上也用 Claude Code，确保不在同一个文件上工作
```

---

## 七、技术栈汇总

```
采集层：Python + B站WBI API + yt-dlp + faster-whisper (CTranslate2)
语音转写：faster-whisper small（CPU: int8, GPU: float16）
云 ASR：硅基流动 SenseVoiceSmall（免费 · multipart/form-data · 50MB限制）
LLM 精炼：DeepSeek V4 Pro（OpenAI SDK）
LLM 问答：DeepSeek V4 Pro（默认）/ MiniMax M2.7（Anthropic SDK）
意图分类：MiniMax M2.7（OpenAI SDK）
SQL 生成：MiniMax M2.7（OpenAI SDK）
Embedding：SiliconFlow API（BAAI/bge-large-zh-v1.5）
RAG 框架：LangChain + BM25 混合检索
后端：FastAPI + Python 3.11
前端：React 18 + TypeScript + Tailwind CSS + Vite
存储：DuckDB（结构化）+ ChromaDB（向量）
部署：Docker Compose（profiles 区分环境 · GPU 直通 · Docker SDK 触发采集）
定时调度：bilibili-cron 容器（docker:cli + crond，每6小时）
通知：QQ Bot
GPU 转录：NVIDIA RTX 4060 · CUDA 12.4 · gpu-service 容器（:8011）
UP主管理：B站 API 解析 + YAML 配置生成（router-agent up_manager）
ASR 成本控制：JSON 设置/用量存储 + 月度预算上限 + 手动开关
AI编程工具：Claude Code + Cursor
```

---

## 八、剩余任务

### Phase 9：混合转录架构（进行中）
- [x] 9.1 NAS 侧定时任务改为只下载（不做转写）
- [x] 9.2 Docker GPU 直通（gpu-service 容器 + RTX 4060）
- [ ] 9.3 飞牛 Sync 同步（NAS downloads → 开发机 downloads）
- [ ] 9.4 开发机 GPU 转录 + 结果回传

### 已完成 Phase
- Phase 1-8：核心系统搭建（133/133 任务）
- Phase 10：前端 UP主管理 + NAS 云 ASR 转写（17/17 任务）
  - UP主管理：前端输入 B站链接 → 解析预览 → 选模型 → 写入 YAML
  - 云 ASR：硅基流动 SenseVoiceSmall（免费）+ 预算上限 + 手动开关
  - 新增 API：7 个端点（UP主 4 + ASR 3）
  - 新增组件：UpManager.tsx、GpuTranscribe ASR 区域

详见 `开发计划.md`。

---

## 九、注意事项

```
- N150 转写时 CPU 占用高，串行处理，不要并发
- B站 Cookie 会过期（code=-352），需监控失败并 QQ 通知
- 转写完成后自动删除 m4a，只保留 txt
- faster-whisper small 约需 2-3GB 内存，设 Docker 内存上限
- 双层 Checkpoint 防呆：downloaded.txt（已下载）+ done_bvid.txt（已转写完成）
- text-to-sql 声称用 CrewAI 但实际是纯 Python 手写 pipeline
- bilibili-monitor 已从 Hermes Skill 改造为独立服务，无 Hermes 依赖
- 意图分类器 category 只能用 31 个有效分类名，话题词放 keywords
- Docker 构建使用国内镜像加速（apt: 阿里云, pip: 清华 TUNA, npm: 淘宝）
- 每次镜像重建后执行 docker builder prune -a -f 清理缓存

内存管理（DDR4 8G）：
- 常驻服务（chromadb + FastAPI 服务 + nginx + cron）：约 6.4G
- bilibili-monitor 仅按需启动，跑完自动退出，释放 4G
- 转写期间峰值约 7-8G，在 8G 范围内可接受
- NAS 支持模块化内存扩展，未来可升级至 16G+
```

---

## 十、新会话启动指南

新会话开始时，读取以下文件即可快速进入状态：

```
读取 开发计划.md 和 开发者快速上手指南.md，从剩余任务继续开发
```

或针对特定任务：

```
读取 开发者快速上手指南.md 第 3.1 节，测试问答功能
读取 开发者快速上手指南.md 第 4 节，排查问题
读取 开发者快速上手指南.md 第 5.1 节，调试意图分类器
```

**关键文件清单：**
- `开发计划.md` — 任务清单和进度
- `开发者快速上手指南.md` — 隐性知识、常见问题、调试技巧
- `README.md` — 项目概述、快速开始
- `CLAUDE.md` — 项目约束、开发规范（自动加载）
- `智能内容分析系统-*.md` — 架构、工作流、部署规划
